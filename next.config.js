// Next.js configuration file. You can keep this minimal at first.
// We’ll add image domains, headers, and other settings later.

 /** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Helps catch potential issues during development
}

module.exports = nextConfig
